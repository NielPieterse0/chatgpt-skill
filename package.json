{
  "name": "chatgpt-skill-adoption",
  "private": true,
  "version": "0.1.0",
  "scripts": {
    "test": "python -m unittest discover -s tests -v",
    "verify": "python -m unittest discover -s tests -v && python scripts/skill_security.py validate --repo . --skip-git"
  }
}
