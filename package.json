{
  "name": "chatgpt-skill",
  "private": true,
  "version": "0.1.0",
  "scripts": {
    "test": "python -m unittest discover -s tests -v",
    "verify-project": "python scripts/project_contract.py validate --repo .",
    "verify-compliance": "python scripts/skill_compliance.py validate --repo .",
    "verify-catalogue-evaluations": "python scripts/catalogue_evaluation.py validate --repo .",
    "verify-plugin-portfolio": "python scripts/plugin_portfolio.py validate --repo .",
    "verify-intake": "python scripts/intake_queue.py validate --repo-root .",
    "setup-hooks": "git config --local core.hooksPath .githooks",
    "sync-workspace-mirror": "python scripts/workspace_skill_mirror.py refresh --repo .",
    "sync-workspace-catalogue": "python scripts/workspace_skill_mirror.py publish --repo .",
    "verify-workspace-mirror": "python scripts/workspace_skill_mirror.py verify --repo .",
    "verify-bootstrap": "python -m unittest discover -s tests -v && npm run verify-project && python scripts/skill_security.py validate --repo . --skip-git",
    "verify": "python -m unittest discover -s tests -v && npm run verify-project && npm run verify-compliance && npm run verify-catalogue-evaluations && npm run verify-plugin-portfolio && npm run verify-intake && npm run verify-workspace-mirror && python scripts/skill_security.py validate --repo . && python scripts/skill_security.py catalog --repo .",
    "catalog": "python scripts/skill_security.py catalog --repo ."
  }
}
