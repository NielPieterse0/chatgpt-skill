# Workspace skill history archive

Snapshot date: 2026-09-02

Sources:
- `C:\Projects\.agents\quarantine`
- `C:\Projects\.agents\retired-skills`

Purpose: retain historical and quarantined skill material as repository evidence only.

These files are **not active skills**. They are intentionally outside both `skills/` and `workspace-skills/` and must not be loaded, published, or admitted into the runtime catalogue without a new explicit review and promotion decision.

`inventory.json` records byte length and SHA-256 for every archived file. The source folders were copied; they were not deleted or modified.