# Develop Workflow Skills Admission

## Authority and decision

Issue #7 requests alignment of the repository's general development skills with the current `kis-mcp` reference packages.

The repository owner approved Tier 2 project-content scope at the active repository root for these workflows. `develop-code` and `develop-docs` may therefore declare `.` because their work can affect project code or documentation in any repository-owned location. Project instructions and host/tool permissions remain authoritative.

## Source

- Source repository: `https://github.com/NielPieterse0/kis-mcp.git`
- Immutable package revision: `5ab2aa1e71852363b0a872e1d9a44f3c70298a42`
- Current source HEAD checked during implementation: `5f9c60c1bdf363207cddd4a5fa24cc4d006b3e6f`
- Both source packages are unchanged between those revisions.

## Scope

- Adopt `develop-code` and `develop-docs` without rewriting their package content.
- Update Tier 2 scope validation for declared repository-relative project paths.
- Add adoption manifests and evaluation definitions/evidence required by repository standards.
- Align Git-aware validation with the current `chatgpt-skill` repository identity.
- Keep the runtime switch disabled; admission and runtime enablement remain separate decisions.

## Acceptance

- Source-package hashes match the current `kis-mcp` packages.
- Tier 2 accepts repository-root project scope and rejects empty or invalid scopes.
- Both skills pass structural, manifest, content-integrity, and repository validation.
- Existing pressure-test evidence is preserved and normalized into repository evaluation records.
- Full repository verification and `git diff --check` pass on the final commit.
- Delivery closes Issue #7 through its intended GitHub development linkage without absorbing unrelated local changes.
